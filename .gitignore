.DS_Store
**/.DS_Store
node_modules/

# 환경변수 — 실제로 쓰는 파일은 .env.local 이다. 예전에는 .env 만 막아두어서
# 한 글자 차이로 키가 커밋될 수 있었다(다행히 커밋된 적은 없다).
.env
.env.*
!.env.example

# VAPID 키를 잠시 파일로 받아둔 적이 있다 — 같은 실수를 반복하지 않도록 막아둔다
vapid-keys*
*.pem
