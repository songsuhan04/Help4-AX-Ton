// 보관 기간을 한곳에 모아둔다. 화면 문구와 실제 삭제가 어긋나면 약관과 다른 동작이 된다.
//
// 파일(음성·영상)은 계속 쌓이면 스토리지를 채운다. 영상은 처음부터 7일로 잡았는데 음성은
// 상한이 없어 무한히 늘고 있었다. 어르신 1명이 하루 한 번, 30초 녹음이 200~400KB이므로
// 열 명이면 1년에 1GB에 가까워진다 — 무료 플랜 스토리지 한도가 그쯤이다.
//
// 음성은 파일만 지우고 행은 남긴다. 받아쓴 내용과 소견은 텍스트라 아주 작고, 위험도 판단과
// 나중에 붙일 개인 기준선 비교가 그 기록을 쓴다. 정작 무거운 것은 오디오뿐이다.
// 개인정보 측면에서도 어르신의 목소리 원본을 계속 들고 있을 이유가 없다.
export const RETENTION = {
  /** 영상편지 — 파일과 기록을 함께 지운다 */
  LETTER_DAYS: 7,
  /** 음성 — 파일만 지우고 받아쓴 내용·소견은 남긴다 */
  VOICE_AUDIO_DAYS: 30,
  /** 미리 만들어둔 그날의 질문·주제 */
  PROMPT_DAYS: 14,
} as const;
