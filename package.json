{
  "name": "callog-api",
  "private": true,
  "description": "Repo-root package.json so Vercel can resolve dependencies for the api/ serverless functions (kept separate from demo/v1's frontend package.json).",
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/web-push": "^3.6.4",
    "@vercel/node": "^5.10.1",
    "typescript": "^5.7.0"
  },
  "allowScripts": {
    "esbuild@0.27.0": true
  }
}
